# figures/

**Endorsement Marker:** T(h)reehouse +EC voyage, Handbook layer. No external endorsement implied.

**Licence:** MIT (see repository `LICENCE`).

Stage-labelled inspection figures.

Naming convention: `stage{N}_{description}.pdf` for finals, `stage{N}_{description}_draft.pdf` for in-progress versions (draft versions gitignored).

Expected artifacts (as voyage proceeds):

- `stage1_jc_validation.pdf` — single-mode propagator validated against JC limit + $n_{\max}$ convergence
- `stage2_p_and_neff.pdf` — population and effective orbital count, same trajectory
- `stage3_qpn_comparison.pdf` — three-panel: $p(t)$, $n_{\text{eff}}(t)$, $\sigma^2_{\text{intrinsic}}$ vs. $\sigma^2_{\text{QPN}}$
- `stage5_cutA_sweep.pdf` — single-mode detuning sweep summary
- `stage6_cutB_sweep.pdf` — two-mode configurations
- `stage7_cutC_commensurability.pdf` — three-mode, three commensurability regimes
- `stage8_h3_scatter.pdf` — $T_{\text{det}}(M=1000)$ vs. saturation $\mathcal{C}$, all parameter points — **the H3 diagnostic plot**
